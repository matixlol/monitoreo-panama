FROM ghcr.io/docling-project/docling-serve-cpu:latest

# Railway routes traffic to the port your process listens on. Docling Serve listens
# on 5001 by default; pin PORT to match and expose it for clearer detection.
ENV PORT=5001
EXPOSE 5001

